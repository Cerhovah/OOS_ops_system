# PLAN

## 운영 규칙

- 구현 전에 이 문서를 갱신하고 모든 작업을 `SPEC.md`의 수용 기준(AC)에 연결한다.
- 한 번에 한 Phase만 진행하며 §10.3 게이트 통과 전 다음 Phase로 넘어가지 않는다.
- §10.4 정지 조건이 생기면 해당 작업을 멈추고 `QUESTIONS.md`에 기록한다. 독립 작업만 계속한다.
- 기술 내부 결정은 `DECISIONS.md`, 사용자에게 의미 있는 변경은 `CHANGELOG.md`, 범위 밖 제안은 `FUTURE.md`에 기록한다.
- 완료 판정에는 구현, 자동 테스트, 수동·실기기 검증, 증빙, 문서 갱신이 모두 필요하다.
- 사용자의 Phase 1 구현 승인 전에는 애플리케이션 코드·설정·의존성을 생성하거나 변경하지 않는다.

## 현재 상태

- 단계: Phase 1 — 초안(v0.1)
- 상태: **구현 승인 대기**
- 문서 기준일: 2026-08-20
- 목표: AC-1~AC-18 전부 통과, §10.3 게이트 통과, 사용자 기기에서 하루치 실제 기록 완료
- 현재 정지 범위: 전체 구현은 사용자 승인 전 대기. AC-1·AC-13·AC-14의 실기기 검증 조건은 `QUESTIONS.md` Q-001 답변 대기
- 코드 상태: 애플리케이션 코드 없음

## Phase 1 구현 순서

승인 후 아래 순서로 진행한다. 순서 변경 시 이 문서와 `CHANGELOG.md`를 함께 갱신한다.

1. 앱 기반과 데이터 계층: AC-1, AC-2, AC-18
2. 항목·일정·기록 기반: AC-4, AC-5
3. 오늘과 오늘 종료: AC-3, AC-6, AC-7
4. 주간과 계획 버전: AC-8, AC-9, AC-10, AC-11
5. 프로젝트와 KPI: AC-12
6. 로컬 알림과 딥링크: AC-13, AC-14
7. 내보내기·오프라인·불변조건 감사: AC-15, AC-16, AC-17
8. 전체 회귀, 실기기 하루 기록, Phase 1 게이트

## Phase 1 — AC-1~AC-18 1:1 구현 계획

- [ ] **AC-1 — 개발 빌드 설치와 아이콘 실행**
  - 구현: Expo Router 기반 앱 셸, 한국어 UI, Asia/Seoul 기본값, 개발 빌드 설정과 앱 아이콘을 구성한다.
  - 완료 조건: Q-001에서 정한 사용자 휴대폰에 설치하고 아이콘으로 콜드 스타트한다.
  - 검증/증빙: TP-AC-01 결과와 설치·실행 화면 기록.

- [ ] **AC-2 — 첫 실행 시드와 관리**
  - 구현: 버전형 SQLite 마이그레이션, 첫 실행 1회 시드, §4.4 계정·항목·프로젝트·KPI·168h 계획을 저장한다. 편집·보관·소프트 삭제 경로를 제공한다.
  - 완료 조건: 재시작해도 시드가 중복되지 않고 모든 시드를 편집·보관·삭제할 수 있다.
  - 검증/증빙: TP-AC-02 자동·수동 결과와 DB 덤프.

- [ ] **AC-3 — 오늘 화면 기록 탭 예산**
  - 구현: 타이머 시작/정지, 완료형, 횟수형, 수동 시간 기록을 오늘 행에서 직접 처리하고 모달 중첩을 금지한다.
  - 완료 조건: 시작·정지·완료·횟수는 각 1탭, 수동 시간은 2~3탭, 기록 1건은 5~10초 이내다.
  - 검증/증빙: TP-AC-03 탭 수·소요 시간 기록과 화면 녹화.

- [ ] **AC-4 — 다섯 항목 유형의 전체 수명주기**
  - 구현: `time`, `completion`, `count`, `numeric`, `event`별 생성·기록 UI와 수정·소프트 삭제·복구를 구현한다.
  - 완료 조건: 각 유형마다 생성 → 기록 → 수정 → 삭제 → 복구가 데이터 손실 없이 끝난다.
  - 검증/증빙: TP-AC-04 유형별 통합·수동 결과.

- [ ] **AC-5 — 일정 규칙과 오늘 항목 중복 방지**
  - 구현: 요일 마스크, 자동 생성, 수동 추가, 진행 중 타이머를 합치는 `todayItems`를 멱등 처리한다.
  - 완료 조건: 통학 등 일정 항목이 지정 요일에만 나타나고 재시작·재조회·수동 추가에도 중복되지 않는다.
  - 검증/증빙: TP-AC-05 단위·통합 결과.

- [ ] **AC-6 — 남은 가용시간과 오늘 합계**
  - 구현: §6의 `remainingAvailableToday`와 오늘 계획·실제 합계를 순수 함수로 계산해 상단에 표시한다.
  - 완료 조건: 미수행 고정 일정, 하루 종료 시각, 진행 기록 변화가 즉시 반영되고 표시값은 0 미만으로 내려가지 않는다.
  - 검증/증빙: TP-AC-06 경계값 단위 테스트와 화면 캡처.

- [ ] **AC-7 — 오늘 종료 스냅샷과 비잠금 동작**
  - 구현: 항목별 계획/실제/차이, 길이 제한 없는 확장형 메모, `day_closures` 스냅샷을 저장한다.
  - 완료 조건: 종료 후에도 기록 추가·수정이 가능하며 저장된 종료 스냅샷을 확인할 수 있다.
  - 검증/증빙: TP-AC-07 통합·수동 결과.

- [ ] **AC-8 — 주간 회계 표와 분해**
  - 구현: 최신 계획 버전과 실제 기록을 계정별 계획/실제/차이 및 총계로 집계하고 항목·요일 분해를 제공한다.
  - 완료 조건: 총계 행과 계정 상세가 선택 주의 원자료와 일치하며 해석·판정 문구가 없다.
  - 검증/증빙: TP-AC-08 집계 단위 테스트, 대조표, 화면 캡처.

- [ ] **AC-9 — 168시간 계획 편집과 비차단 저장**
  - 구현: 계정별 계획 편집 시 합계와 초과/미배분 배지를 실시간 계산하고 `조정하기`와 `그대로 저장`을 제공한다.
  - 완료 조건: 168h 미만·초과 모두 경고 후 저장할 수 있고 형식상 완전한 입력의 저장 버튼을 차단하지 않는다.
  - 검증/증빙: TP-AC-09 단위·수동 결과.

- [ ] **AC-10 — append-only 계획 버전과 복원**
  - 구현: 계획 저장마다 version을 증가시키고 과거 버전 열람·복원을 제공한다. 복원도 새 버전으로 저장한다.
  - 완료 조건: 과거 행을 덮어쓰지 않고 전체 버전과 source를 추적할 수 있다.
  - 검증/증빙: TP-AC-10 repository 통합 테스트와 DB 덤프.

- [ ] **AC-11 — 지난주 계획 1탭 복사**
  - 구현: 다음 주 계획이 없을 때 지난주 최신 계획을 `copy_last_week` source의 새 버전으로 복사한다.
  - 완료 조건: 사용자 동작 1탭으로 복사되고 원본과 기존 버전은 변하지 않는다.
  - 검증/증빙: TP-AC-11 통합·수동 결과.

- [ ] **AC-12 — 프로젝트·KPI와 파생 시간**
  - 구현: 프로젝트 생성, 기본/사용자 정의 KPI 선택·추가, KPI 값 기록, 연결 시간형 항목의 누적·이번 주 시간을 파생한다.
  - 완료 조건: KPI 값은 카드에서 1~2탭으로 기록되고 파생 시간이 원기록 합계와 일치한다.
  - 검증/증빙: TP-AC-12 집계 단위·통합 테스트와 탭 수 기록.

- [ ] **AC-13 — 일일 로컬 알림과 콜드 스타트 딥링크**
  - 구현: 기본 21:30 일일 로컬 알림, 종료 완료 시 취소 규칙, 알림 응답 기반 `/today/close` 라우팅을 구현한다.
  - 완료 조건: 앱이 종료된 실기기에서 알림을 탭하면 오늘 종료 화면으로 이동한다.
  - 검증/증빙: TP-AC-13 실기기 화면 녹화와 예약 상태 기록.

- [ ] **AC-14 — Android 채널·권한·재예약**
  - 구현: importance HIGH 채널, 온보딩 1회 권한 요청, 설정 재요청 경로, 예약 ID 보관, 시작 시 점검·재예약을 구현한다.
  - 완료 조건: 권한 허용·거부·재요청과 앱 재시작 후 예약 복구가 Android 개발 빌드에서 확인된다.
  - 검증/증빙: TP-AC-14 Android 개발 빌드 결과와 시스템 설정 캡처.

- [ ] **AC-15 — 전체 JSON과 테이블별 CSV 내보내기**
  - 구현: 모든 Phase 1 테이블을 JSON 전체 및 CSV 테이블별 파일로 내보내며 소프트 삭제 행과 계획 전 버전을 포함한다.
  - 완료 조건: 내보낸 행 수·ID·버전이 SQLite 원본과 일치하고 공유/저장 실패가 원본을 변경하지 않는다.
  - 검증/증빙: TP-AC-15 자동 대조 결과와 표본 내보내기 파일.

- [ ] **AC-16 — 완전한 오프라인 기록**
  - 구현: Phase 1의 기록·조회·수정·삭제·복구·계획·프로젝트·종료 흐름이 네트워크에 의존하지 않게 한다.
  - 완료 조건: 비행기 모드에서 핵심 기록 시나리오가 끝나고 재시작 후 데이터가 유지된다.
  - 검증/증빙: TP-AC-16 실기기 체크리스트와 재시작 전후 DB 대조.

- [ ] **AC-17 — 금지 문구·게임화 부재**
  - 구현: §12 정보형 문구만 사용하고 점수·등급·연속일수·칭찬·질책·사용자 서술을 넣지 않는다.
  - 완료 조건: 전체 UI, 알림, 빈 상태, 오류 메시지, 시드 문구의 수동 감사에서 금지 요소가 0건이다.
  - 검증/증빙: TP-AC-17 전 화면 문구 감사표.

- [ ] **AC-18 — §6 계산 규칙 단위 테스트**
  - 구현: 계산을 UI와 분리한 순수 함수로 두고 주 범위, 최신 계획, 실제/차이, 오늘 항목, 남은 시간, 타이머, 168h 경고를 테스트한다.
  - 완료 조건: 주 경계, 자정 넘는 타이머의 시작일 귀속, 168h 합계, 차이, 요일 템플릿, 남은 가용시간 테스트가 통과하고 도메인 모듈 커버리지 90% 이상이다.
  - 검증/증빙: TP-AC-18 테스트·커버리지 보고서.

## Phase 1 게이트 통과 증빙

아직 실행된 검증은 없다. 구현 승인 후 `TESTPLAN.md` 결과표와 아래 칸을 실제 경로·명령 출력으로 채운다.

| 게이트 | 상태 | 증빙 |
|---|---|---|
| AC-1~AC-18 | 대기 | `TESTPLAN.md`의 TP-AC-01~TP-AC-18 |
| TypeScript `tsc --noEmit` 오류 0 | 대기 | 미생성 |
| ESLint 오류 0 | 대기 | 미생성 |
| 도메인 단위 테스트·커버리지 90% 이상 | 대기 | 미생성 |
| 통합 테스트 | 대기 | 미생성 |
| `expo-doctor` 경고 검토 | 대기 | 미생성 |
| 개발 빌드·실기기 수동 테스트 | Q-001 답변 대기 | 미생성 |
| 사용자 기기 하루치 실제 기록 | Q-001 답변 대기 | 미생성 |
| 문서 갱신·§10.7 보고 | 대기 | 이 문서 및 관련 문서 |

## 후속 Phase

- [ ] **Phase 2 — 동기화:** AC-19~AC-22. 사용자 승인과 인증 방식 결정 후 Supabase 백업·복구·오프라인 동기화·RLS를 구현한다.
- [ ] **Phase 3 — Telegram:** AC-23~AC-26. 사용자 승인과 봇 자격증명 준비 후 보조 인터페이스를 구현한다.
- [ ] **Phase 4 — 분석:** AC-27~AC-30. 사용자 승인과 AI 제공자 결정 후 분석 세션과 승인형 제안 적용을 구현한다.
- [ ] **Phase 5 — 확장:** §3.1 L과 승인된 `FUTURE.md` 항목. 현재 명세에는 수용 기준이 없으므로 착수 전에 사용자와 AC를 정의한다.

## 승인 후 첫 작업

1. Expo/TypeScript 개발 빌드 기반과 테스트 하네스의 기술 결정을 `DECISIONS.md`에 기록한다.
2. AC-1·AC-2·AC-18을 위한 앱 셸, 마이그레이션/시드, 순수 계산 모듈부터 구현한다.
3. 자동 검증 결과를 `TESTPLAN.md`, 사용자 의미 변경을 `CHANGELOG.md`에 누적한다.
